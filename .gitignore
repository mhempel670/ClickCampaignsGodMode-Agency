# API keys and tokens — never commit these
.env
.mcp.json

# OS files
.DS_Store
Thumbs.db

# Node modules (if any scripts are added later)
node_modules/
.pnpm-store/

# Maintained in its own private repository
clients/graybeard-guidance/campaigns/graybeard-master-document-app/output-assets/app/
